// Translation fragment: logStream (en). Keys are namespaced 'logStream.xxx' — see frontend/src/lib/i18n.ts.

export default {
    'logStream.filterPlaceholder': 'Filter log… (Ctrl+F)',
    'logStream.clearFilterTitle': 'Clear filter (Esc)',
    'logStream.renderMarkdownTitle': 'Render markdown (headings, lists, tables, code) instead of raw text',
    'logStream.markdownLabel': 'Markdown',
    'logStream.noEntriesMatch': 'No entries match',
    'logStream.noLogEntries': 'No log entries yet.',
    'logStream.collapse': 'Collapse',
    'logStream.expand': 'Expand',
    'logStream.showAsRaw': 'Show this entry as raw text',
    'logStream.renderAsMarkdown': 'Render this entry as markdown',
    'logStream.jumpToLatest': 'Jump to latest',
} as Record<string, string>;
